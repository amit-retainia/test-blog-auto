{
  "name": "dobby-blog-demo",
  "private": true,
  "type": "module",
  "description": "Static blog site. This package.json exists only for scripts/upload-image.mjs — the site itself has no build step.",
  "scripts": {
    "upload": "node --env-file=.env scripts/upload-image.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
